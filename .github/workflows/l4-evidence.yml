# Machine-produced plugin release evidence (ADR-0067 — the CI leg of the hybrid;
# RFC-036 accepted 2026-07-25). Runs the four L4 scenarios on a GitHub runner
# authenticated by DeepSeek env auth (spike plugin#45: full pipeline observed
# working, marketplace install included). The operator's Claude OAuth NEVER
# appears here — laptop runs via scenarios/release.mjs remain the occasional
# full-fidelity check.
#
# ─── WHY TWO JOBS (the vfkb-pi-package shape, ratified in ADR-0067 D3) ─────────
#   produce — holds DEEPSEEK_TOKEN, `contents: read`. Runs the scenarios,
#             uploads the records. Its GITHUB_TOKEN cannot push.
#   vouch   — `contents: write`, NO secret. Re-derives every verdict with the
#             gate's own functions (verdict + tree-binding + provenance) and
#             commits the records to the DISPATCHED REF only if all pass.
# No single job can both spend the key and push. Honest limit (same as the pi
# package): vouch trusts produce's observations — it has no independent oracle.
#
# ─── SECRET SAFETY (ADR-0067 D3) ──────────────────────────────────────────────
# Trigger is workflow_dispatch ONLY — never pull_request — so no fork ever sees
# the key, and dispatch requires write access. Records are redacted at capture
# (scenarios/auth.mjs redactSecrets): GitHub masks logs, not artifact/file
# contents.
#
# ─── AUTH FAILURE SHAPE (ADR-0067 D4, observed in the spike) ──────────────────
# A bad token does not error — `claude -p` HANGS to the per-turn timeout. The
# scenarios carry per-turn timeouts; a first-turn timeout should be read as
# auth-shaped, not model flake. Flake policy: one automatic retry per scenario,
# then fail LOUDLY — never a third try, never a relaxed threshold.
name: l4-evidence

on:
  workflow_dispatch:
    inputs:
      only:
        description: 'Scenario to run (all | brief-skill | hooks-smoke | inactive-signal | install-path)'
        type: string
        default: 'all'
      trials:
        description: 'Trials per arm (ADR-0022 needs >=3 for a release record)'
        type: string
        default: '3'
      commit:
        description: 'Vouch: commit the machine-produced records back to the dispatched ref'
        type: boolean
        default: true

# Metered, and the scenarios say "one at a time" — serialize.
concurrency:
  group: l4-evidence
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  produce:
    runs-on: ubuntu-latest
    # Summed per-turn caps at trials=3 across all four scenarios are ~230 min
    # BEFORE any D4 retry (reviewer arithmetic, PR #46) — a job-timeout
    # cancellation vouches nothing yet burns the full metered spend, so the
    # budget must exceed the caps, not the hoped-for runtime.
    timeout-minutes: 300
    permissions:
      contents: read # holds the key, cannot push
    steps:
      - uses: actions/checkout@v4
        with:
          # install-path resolves release tags (prevReleaseWithoutBrief) and the
          # ref under test; both need full history + tags.
          fetch-depth: 0
          fetch-tags: true
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install Claude Code (the CLI version the spike verified)
        run: npm install -g @anthropic-ai/claude-code@2.1.220
      - name: Route SSH github clones over HTTPS, SYSTEM-wide
        # `claude plugin marketplace add owner/repo` clones git@github.com:…,
        # and the scenarios run it under sandbox HOMEs — which never see a
        # user-level gitconfig. --system lands in /etc/gitconfig, which every
        # HOME inherits. A runner has no SSH key; without this every arm would
        # die in SETUP.
        run: sudo git config --system url."https://github.com/".insteadOf "git@github.com:"
      - name: Produce the records (metered — real agent sessions on DeepSeek)
        env:
          DEEPSEEK_TOKEN: ${{ secrets.DEEPSEEK_TOKEN }} # the ONLY step that sees it
          VFKB_L4_AUTH: deepseek-env
          VFKB_IP_REF: ${{ github.ref_name }}
          VFKB_HS_MODEL: deepseek-v4-pro
          VFKB_IS_MODEL: deepseek-v4-pro
          VFKB_BS_OUTER_MODEL: deepseek-v4-pro
          VFKB_IP_OUTER_MODEL: deepseek-v4-pro
          VFKB_BS_TRIALS: ${{ github.event.inputs.trials }}
          VFKB_HS_TRIALS: ${{ github.event.inputs.trials }}
          VFKB_IS_TRIALS: ${{ github.event.inputs.trials }}
          VFKB_IP_TRIALS: ${{ github.event.inputs.trials }}
          # Never interpolate an input into script text — env-var it (injection
          # hardening; dispatch already needs write access, this is depth).
          ONLY: ${{ github.event.inputs.only }}
        run: |
          set -uo pipefail
          case "$ONLY" in
            all) scenarios="brief-skill hooks-smoke inactive-signal install-path" ;;
            brief-skill|hooks-smoke|inactive-signal|install-path) scenarios="$ONLY" ;;
            *) echo "::error::unknown scenario '$ONLY'"; exit 1 ;;
          esac
          for s in $scenarios; do
            echo "::group::scenario $s"
            if ! node "scenarios/$s.mjs"; then
              # ADR-0067 D4: one automatic retry, then loud failure. A fully red
              # run on the second try is EVIDENCE that something is wrong.
              echo "::warning::$s was not DEMONSTRATED on the first run — retrying ONCE (ADR-0067 D4)"
              if ! node "scenarios/$s.mjs"; then
                echo "::error::$s failed twice — not retrying again; read the record, do not relax the threshold"
                echo "::endgroup::"
                exit 1
              fi
            fi
            echo "::endgroup::"
          done
      - name: Recompute every produced verdict (records do not assert their own)
        run: |
          node --input-type=module -e '
            const { verdict } = await import("./scenarios/release-gate.mjs");
            const { readFileSync, readdirSync } = await import("node:fs");
            let bad = 0;
            for (const f of readdirSync("scenarios/records").filter((f) => f.endsWith(".json"))) {
              const rec = JSON.parse(readFileSync(`scenarios/records/${f}`, "utf8"));
              const v = verdict(rec);
              console.log(`${f}: ${v.ok ? "DEMONSTRATED (recomputed)" : "NOT demonstrated: " + v.reasons.join("; ")}`);
              if (!v.ok) bad++;
            }
            process.exit(bad ? 1 : 0);
          '
      - name: Upload the machine-produced records
        uses: actions/upload-artifact@v4
        with:
          name: l4-records
          path: scenarios/records/*.json
          if-no-files-found: error

  vouch:
    needs: produce
    if: ${{ github.event.inputs.commit == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write # can push, holds NO secret
      pull-requests: write # ADR-0068: enable auto-merge (an intent, not a merge)
      actions: write # `gh workflow run release-tag.yml` — POST .../dispatches
                     # is an Actions-write endpoint; unlisted permissions default
                     # to none, so without this the dispatch 403s.
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0
          fetch-tags: true
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Fetch the machine-produced records
        uses: actions/download-artifact@v4
        with:
          name: l4-records
          path: scenarios/records/
      - name: Verify with the gate's own functions, then commit ONLY if clean
        run: |
          set -euo pipefail
          # Only records this run CHANGED are candidates — the produce artifact
          # sweeps the whole records dir, so validating every file would refuse
          # a partial (`only=<scenario>`) dispatch because the UNTOUCHED
          # committed laptop records predate producedBy. Stale-but-unchanged
          # records are the gate's business (phased flag), not this run's.
          # `status --porcelain` (not `diff`) so a brand-new untracked record
          # from a future fifth scenario cannot slip past as "unchanged".
          changed=$(git status --porcelain -- scenarios/records | awk '{print $2}')
          if [ -z "$changed" ]; then
            echo "records unchanged — nothing to vouch"; exit 0
          fi
          echo "changed records:"; echo "$changed"
          # Re-derive on every CHANGED record — the committing job must not take
          # the producing job's word for it. verdict() reads per-trial
          # observations; treeBindingReasons() binds the record to THIS
          # checkout's plugin/ tree; provenanceReasons() enforces ADR-0067 D5 on
          # everything entering via this path, regardless of the phased flag.
          CHANGED="$changed" node --input-type=module -e '
            const { verdict, treeBindingReasons, provenanceReasons } = await import("./scenarios/release-gate.mjs");
            const { readFileSync } = await import("node:fs");
            let bad = 0;
            for (const path of process.env.CHANGED.split("\n").filter(Boolean)) {
              const slug = path.replace(/^scenarios\/records\//, "").replace(/\.json$/, "");
              const rec = JSON.parse(readFileSync(path, "utf8"));
              const reasons = [
                ...verdict(rec).reasons,
                ...treeBindingReasons(".", rec, slug),
                ...provenanceReasons(rec),
              ];
              if (reasons.length) { console.error(`refusing to vouch ${slug}:\n  ` + reasons.join("\n  ")); bad++; }
              else console.log(`${slug}: DEMONSTRATED, tree-bound, provenanced — vouchable`);
            }
            process.exit(bad ? 1 : 0);
          '
          git config user.name  "vfkb-release-bot"
          git config user.email "vfkb-release-bot@users.noreply.github.com"
          git add -- scenarios/records
          git commit \
            -m "chore: re-pin machine-produced L4 evidence [skip ci]" \
            -m "Auto-vouched by CI (ADR-0067, run ${{ github.run_id }}): DeepSeek-authenticated scenarios, every verdict recomputed, tree-bound, provenanced. Pathspec-scoped to scenarios/records." \
            -m "Scope (ADR-0067): these records prove harness wiring on deepseek-v4-pro, not the production model config — that is the laptop leg's job."
          git push origin "HEAD:${{ github.ref_name }}"
          echo "vouched: committed the machine-produced records to ${{ github.ref_name }}"

      # ADR-0068 — a green release PR merges itself. GitHub's NATIVE auto-merge
      # is the mechanism on purpose: it merges only when the branch's required
      # checks pass, so BRANCH PROTECTION stays the enforcement and a red PR
      # cannot merge. This step states an intent; it never presses merge.
      #
      # SCOPE GUARD (ADR-0068 §5): release PRs only. The dispatch ref is
      # whatever a maintainer typed, and `release-gate` — the sole required
      # check — runs on EVERY pull_request, so without this guard dispatching
      # evidence on a feature branch would auto-merge that feature PR, skipping
      # the ADR-0052 review gate (which is a process step, not a required check).
      - name: Enable auto-merge on the release PR (ADR-0068)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          # The patterns are the ones this repo has ACTUALLY used for releases,
          # checked against merged-PR history: re-vendor/engine (v0.6.0-v0.13.0),
          # chore/revendor-0.11.0, chore/revendor-0.12.0, chore/re-vendor-0.5.0,
          # repin/*. A guard encoding a convention the repo does not follow would
          # silently no-op self-merge on the next release — fails safe, but a
          # capability that works on a coin flip is not the one ADR-0068 describes.
          case "$BRANCH" in
            release/*|re-vendor/*|repin/*|chore/re-vendor*|chore/revendor*) ;;
            *) echo "::notice::$BRANCH is not a release branch — ADR-0068 §5 scopes self-merge to release PRs; not queueing"; exit 0 ;;
          esac
          # Distinguish "gh failed" from "no eligible PR": swallowing both into an
          # empty string reports a network error as a deliberate skip.
          if ! prs=$(gh pr list --head "$BRANCH" --state open --json number,isDraft,labels); then
            echo "::error::gh pr list failed for $BRANCH — cannot tell whether a PR is eligible"; exit 1
          fi
          # Filter BEFORE indexing: `.[0] | select(...)` hides an eligible PR
          # behind a draft/held one when several share a head.
          pr=$(echo "$prs" | jq -r '[.[] | select(.isDraft==false) | select([.labels[].name] | index("hold") | not)] | .[0].number // empty')
          if [ -z "$pr" ]; then
            echo "no eligible open PR for $BRANCH (absent, draft, or labelled hold) — nothing to queue"
            exit 0
          fi
          gh pr merge "$pr" --squash --auto
          # TWO legitimate outcomes, and asserting only one of them fires a false
          # red on the commoner path: `gh` does NOT enable auto-merge when the PR
          # is already mergeable (v2.68.1 merge.go: `AutoMergeEnable && !isImmediatelyMergeable`)
          # — it merges on the spot, leaving autoMergeRequest null and state MERGED.
          # Accept MERGED-now or ENABLED-and-open; fail only when neither holds.
          # (autoMergeRequest is NOT cleared on merge, so it alone cannot tell
          # queued from merged either.)
          if ! observed=$(gh pr view "$pr" --json autoMergeRequest,state --jq '(if .autoMergeRequest then "enabled" else "none" end) + " " + .state'); then
            echo "::error::gh pr merge --auto succeeded but the PR state could not be observed"; exit 1
          fi
          read -r amr state <<<"$observed"
          if [ "$state" = "MERGED" ]; then
            echo "::notice::PR #${pr}: MERGED IMMEDIATELY — the required checks were already green"
            echo "MERGED_NOW=1" >> "$GITHUB_ENV"
          elif [ "$amr" = "enabled" ] && [ "$state" = "OPEN" ]; then
            echo "::notice::PR #${pr}: auto-merge ENABLED (queued) — it will merge if and only if the required checks pass"
          else
            echo "::error::PR #${pr}: neither merged nor auto-merge-enabled (autoMergeRequest=$amr state=$state)"; exit 1
          fi

      # THE TAG DOES NOT CREATE ITSELF AFTER A CI MERGE, and finding that out in
      # production would have been a silent broken release (review PR #48, B-2).
      # `release-tag.yml` triggers on `push: main` only, and GitHub does not run
      # push-triggered workflows for a push made with the repository's
      # GITHUB_TOKEN — which is what an auto-merge performed by this job is. So
      # the chain ADR-0068 claims ("merged -> tagged") breaks exactly on the CI
      # leg unless the tag job is asked for explicitly and the result OBSERVED.
      - name: Ask for the tag, then observe it on origin (ADR-0068 / ADR-0060)
        if: ${{ env.MERGED_NOW == '1' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          name=$(node -p "require('./plugin/.claude-plugin/plugin.json').name")
          version=$(node -p "require('./plugin/.claude-plugin/plugin.json').version")
          tag="${name}--v${version}"
          if [ -n "$(git ls-remote --tags origin "refs/tags/${tag}")" ]; then
            echo "::notice::${tag} already on origin — nothing to do"; exit 0
          fi
          # NOT `|| true`: a 403 here (missing actions: write) would otherwise
          # masquerade 5 minutes later as "the tag never appeared", pointing the
          # operator at the wrong cause.
          if ! gh workflow run release-tag.yml --ref main; then
            echo "::error::could not dispatch release-tag.yml — the tag will not be created. Check the job's actions: write permission."
            exit 1
          fi
          # Content assertion, not a green job: the ref either exists on the
          # remote or this release is untagged and someone has to know.
          for i in $(seq 1 20); do
            if [ -n "$(git ls-remote --tags origin "refs/tags/${tag}")" ]; then
              echo "::notice::observed ${tag} on origin"; exit 0
            fi
            sleep 15
          done
          echo "::error::PR merged but ${tag} is NOT on origin. A GITHUB_TOKEN merge does not trigger release-tag.yml; the dispatch above did not produce it either. Recover: gh workflow run release-tag.yml --ref main"
          exit 1
